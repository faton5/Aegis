# Dépendances principales
discord.py>=2.3.0
python-dotenv>=1.0.0
supabase>=2.3.0  # Pour la base de données centralisée

# Dépendances optionnelles pour les nouvelles fonctionnalités
aiofiles>=23.0.0  # Pour la gestion asynchrone des fichiers
cryptography>=41.0.0  # Pour le chiffrement des données sensibles

# Dépendances de développement (optionnelles)
pytest>=7.0.0  # Pour les tests
black>=23.0.0  # Pour le formatage du code
flake8>=6.0.0  # Pour le linting